# Single-arch overlay: linux/amd64 (Intel/AMD, Synology x86_64).
include:
  - path: docker-compose.yml

services:
  pa:
    build:
      context: .
      platforms:
        - linux/amd64
      args:
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        BUILD_TIME: ${BUILD_TIME:-unknown}
