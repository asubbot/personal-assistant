// Package logredact provides built-in and configurable redaction patterns so that
// secret values (API keys, tokens, etc.) are replaced by a placeholder before data
// is written to LLM logs or application logs (REQ-01.026, REQ-01.027, REQ-01.028).
package logredact
