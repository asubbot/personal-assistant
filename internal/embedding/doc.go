// Package embedding provides the dedicated embedder for vector store (vector memory).
// It is separate from the chat LLM provider. Use config.Embedding (EmbeddingProvider) to configure.
package embedding
