// Package llm implements the LLM provider interface and implementations.
// Provider.Complete(ctx, messages, opts) performs one chat completion (OpenAI-compatible HTTP).
// Use NewProvider(config.LLMProvider) for openai, openai-compatible, or ollama (same HTTP API).
package llm
