tools:
  - id: example_cmd
    index_text: Run an example command on a node.
    template: "echo {{message}}"
    node_id: nas
    arguments:
      - name: message
        type: string
        required: true
