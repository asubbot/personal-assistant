# Single-arch overlay: linux/arm64 (Apple Silicon, ARM64 NAS/servers).
include:
  - path: docker-compose.yml

services:
  pa:
    build:
      context: .
      platforms:
        - linux/arm64
      args:
        GIT_COMMIT: ${GIT_COMMIT:-unknown}
        BUILD_TIME: ${BUILD_TIME:-unknown}
