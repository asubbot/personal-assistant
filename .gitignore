# ai-sdlc nested repo
ai-sdlc/

# Go
*.out
coverage.html
# Test binaries from `go test -c` (e.g. repo-root llm.test, package.test)
*.test

# Editor / scratch files (e.g. *.json.tmp next to testdata)
*.tmp

# Binary / output named pa at repo root only (not cmd/pa)
/pa
bin/

# Local vector index files at repo root (dev / accidental cwd)
/pa_vectors.sqlite
/pa_vectors.sqlite-shm
/pa_vectors.sqlite-wal

# Built CLI binaries (e.g. make validate → bin/validate)
/bin/

# Environment and secrets
.env
.secrets/
.vscode/
.cursor

# Operator-local config (templates live in config.examples/)
.config/
.data/

# OS
.DS_Store
Thumbs.db

# Integration test temp dir
tests/integration/tmp/
