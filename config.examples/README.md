# Configuration templates

Copy these files into **`.config/`** at the repository root (gitignored). Set **`PA_CONFIG_DIR`** if you use a different directory (default is **`./.config`**). See [README.md](../README.md) and [docs/configuration.md](../docs/configuration.md).
